doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href=" /css/normalize.css")
        script(src='https://kit.fontawesome.com/0f4fa5e38a.js' crossorigin='anonymous')
        link(rel="stylesheet", href="/css/app.css")
        title #{nombrePagina}
    body 
        header.barra    
            h1 Uptask - Administrador de Proyectos 
            a(href="#") Cerrar Sesión 

        main.contenedor
            aside.contenedor-proyectos
                .panel.crear-proyecto  
                    a(href="/nuevo-proyecto" class="boton") New Proyect 
                        span +
                .panel.lista-proyectos
                    ul#proyectos.proyectos
                        if(proyectos.length)
                            each proyecto in proyectos 
                                li 
                                    a(href=`/proyectos/${proyecto.url}`)= proyecto.nombre 

            block contenido
            
            //-  pre=vardump(proyectos)
        script(src="/dist/bundle.js")